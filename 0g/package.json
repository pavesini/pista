{
  "name": "0g",
  "version": "1.0.0",
  "description": "TEE script that cals 0g AI model",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "poll": "node index.js",
    "docker-build": "docker build -t 0g-aml .",
    "docker-run": "docker run --env-file .env --name 0g-aml 0g-aml",
    "docker-stop": "docker stop 0g-aml && docker rm 0g-aml",
    "docker-rebuild": "docker rm 0g-aml && docker build -t 0g-aml ."
  },
  "author": "Conio",
  "license": "ISC",
  "dependencies": {
    "@clickhouse/client": "^1.23.1",
    "dotenv": "^17.4.2",
    "openai": "^6.49.0"
  }
}
